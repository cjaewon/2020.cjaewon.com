import React from 'react';
import { css } from '@emotion/core';

const Projects: React.FC<{}> = () => {
  return (
    <section css={styles}>

    </section>
  );
};

const styles = css`
  height: 100vh;
`;

export default Projects;